# Code generated by cmd/generate-gwapi; DO NOT EDIT.

tests-GW-API-1_3_0:
  retry: 2
  needs: ["diff", "tidy"]
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: "$CI_PROJECT_NAMESPACE != 'haproxy-controller/community' && $CI_PIPELINE_SOURCE == 'push'"
  stage: unit-tests
  image:
    name: $HAPROXY_REGISTRY_GO/haproxy-alpine:$HAPROXY_VERSION-go$GO_VERSION
    entrypoint: [""]
  tags:
    - go
  variables:
    METRICS_OUTPUT_DIR: ${CI_PROJECT_DIR}/metrics-output/gwapi-1_3_0
  before_script:
    - export PATH=$PATH:/root/go/bin
    - mkdir -p $METRICS_OUTPUT_DIR
  script:
    - GWAPI_VERSION=1.3.0 task test
  artifacts:
    when: always
    paths:
      - junit-report.xml
      - metrics-output/gwapi-1_3_0
    reports:
      junit: junit-report.xml

tests-GW-API-1_4_0:
  retry: 2
  needs: ["diff", "tidy"]
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: "$CI_PROJECT_NAMESPACE != 'haproxy-controller/community' && $CI_PIPELINE_SOURCE == 'push'"
  stage: unit-tests
  image:
    name: $HAPROXY_REGISTRY_GO/haproxy-alpine:$HAPROXY_VERSION-go$GO_VERSION
    entrypoint: [""]
  tags:
    - go
  variables:
    METRICS_OUTPUT_DIR: ${CI_PROJECT_DIR}/metrics-output/gwapi-1_4_0
  before_script:
    - export PATH=$PATH:/root/go/bin
    - mkdir -p $METRICS_OUTPUT_DIR
  script:
    - GWAPI_VERSION=1.4.0 task test
  artifacts:
    when: always
    paths:
      - junit-report.xml
      - metrics-output/gwapi-1_4_0
    reports:
      junit: junit-report.xml

tests-GW-API-1_5_0:
  retry: 2
  needs: ["diff", "tidy"]
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: "$CI_PROJECT_NAMESPACE != 'haproxy-controller/community' && $CI_PIPELINE_SOURCE == 'push'"
  stage: unit-tests
  image:
    name: $HAPROXY_REGISTRY_GO/haproxy-alpine:$HAPROXY_VERSION-go$GO_VERSION
    entrypoint: [""]
  tags:
    - go
  variables:
    METRICS_OUTPUT_DIR: ${CI_PROJECT_DIR}/metrics-output/gwapi-1_5_0
  before_script:
    - export PATH=$PATH:/root/go/bin
    - mkdir -p $METRICS_OUTPUT_DIR
  script:
    - GWAPI_VERSION=1.5.0 task test
  artifacts:
    when: always
    paths:
      - junit-report.xml
      - metrics-output/gwapi-1_5_0
    reports:
      junit: junit-report.xml
