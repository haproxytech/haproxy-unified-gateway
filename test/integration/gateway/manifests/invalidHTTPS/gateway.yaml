apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway
spec:
  gatewayClassName: haproxy
  listeners:
  - name: http
    port: 8080
    protocol: HTTP
    hostname: "*.haproxy.local"
  - name: https
    protocol: HTTPS
    port: 8443
   # tls missing with protocol HTTPS
