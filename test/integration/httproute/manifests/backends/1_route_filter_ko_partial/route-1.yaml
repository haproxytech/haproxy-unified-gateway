apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: route-echo-1
spec:
  parentRefs:
  - name: gateway
    sectionName: http
  hostnames:
  - "example.haproxy"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /valid
    backendRefs:
    - name: http-echo-1
      port: 80
  - matches:
    - path:
        type: PathPrefix
        value: /invalid
    filters:
    - type: URLRewrite
      urlRewrite:
        hostname: new.haproxy
    backendRefs:
    - name: http-echo-1
      port: 80
      filters:
      - type: RequestRedirect
        requestRedirect:
          scheme: https
          statusCode: 301
